source 'https://rubygems.org'

gem 'rails', '3.2.6'
gem 'pg', :platform => :ruby
gem 'json'
gem 'haml'
gem 'devise'
gem 'faker'
gem 'simple_form'
gem 'state_machine'
gem 'jquery-rails'
gem 'will_paginate'
gem 'will_paginate-bootstrap'
gem 'state_machine'
gem 'simple-navigation'
gem 'chosen-rails'
gem 'active_attr'
gem 'rabl'
gem 'spreadsheet'

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'coffee-rails', '~> 3.2.1'
  gem 'uglifier', '>= 1.0.3'
end

# Moved out of assets group to fix Heroku deployment issue.
gem 'bootstrap-sass'
gem 'sass-rails',   '~> 3.2.3'

group :development, :test do
  gem 'rspec-rails'
end

group :test do
  gem 'webrat'
  gem 'spork'
end
